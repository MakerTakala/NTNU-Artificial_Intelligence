#include "algo.h"

void IDS_searcj(vector<int> &ans) {


}

vector<int> IDS(bitset<1000> board, size_t size, fstream &out) {
    cout<<"IDS"<<endl;

    for(int deep = 2; ; deep++) {


    }

    return vector<int>();
}

vector<int> IDA(bitset<1000> board, size_t size, fstream &out) {
    cout<<"IDA"<<endl;
    return vector<int>();
}
